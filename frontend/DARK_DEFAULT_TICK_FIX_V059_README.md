# DevBareun Frontend v0.5.9 — Default Dark + Tick Fix

Changes:
- Site starts in dark mode on every page load.
- Old saved light-mode preference is cleared at startup.
- Theme toggle still works during the current session.
- Selected analysis card check badge no longer covers the title text.
- Analysis card title now has safe right padding and a smaller top-right check badge.

Backend is unchanged from the latest v0.5.7 PDF metric fix in the full package.
