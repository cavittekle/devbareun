# DevBareun v1.0.5 — Compact manual fields UI

## Change

The optional missing-data/manual-input area on the upload mapping screen was too tall and visually heavy.

## Fix

- Replaced the three separate warning rows with one compact collapsible card.
- Manual inputs are hidden by default and opened only when the user wants to enter values manually.
- Mobile layout remains single-column and readable.
- No backend calculation logic changed.
