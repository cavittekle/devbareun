# DevBareun v0.5.6 Frontend Fixes

- Keeps approved upload UI design.
- Makes analysis-type binding robust by targeting `[data-analysis-type]` and normalizing `.analysis-type-card` classes.
- Updates backend connection documentation to the current Railway endpoint and v0.5.6 health check.
- Result dashboard fallback text remains generic; backend JSON overwrites KPI values after generated analysis.

Backend must be deployed separately to Railway for preflight and F-2 safety fixes to be live.
